# .agents/workflows DOX

## Purpose

The `.agents/workflows/` directory contains step-by-step markdown workflows triggered by slash commands (e.g. `/plan`, `/coordinate`, `/create`, `/debug`).

## Ownership

- 13 slash command workflow guides (`plan.md`, `coordinate.md`, `create.md`, `debug.md`, etc.).

## Local Contracts

- Clear step-by-step instructions for complex multi-phase tasks.

## Work Guidance

- Follow workflow steps sequentially when slash commands are invoked by the user.

## Verification

- **Doctor Check**: `node .agents/hooks/antigravity-doctor.mjs`

## Child DOX Index

- No nested child directories in `.agents/workflows/`.
